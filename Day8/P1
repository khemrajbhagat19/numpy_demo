import numpy as np
from scipy import linalg

A=np.array([[1,2],[3,4]])
b=np.array([5,6])

# Solve Ax=b
x=linalg.solve(A,b)
print(x)

# determinant
det=linalg.det(A)
print(det)

# Inverse
inv=linalg.inv(A)
print(inv)